import { MaDialogDirective } from './ma-dialog.directive';

describe('MaDialogDirective', () => {
  it('should create an instance', () => {
    // const directive = new MaDialogDirective();
    // expect(directive).toBeTruthy();
  });
});
