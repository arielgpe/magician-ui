import { MaTemplate } from './ma-template.directive';

describe('MaTemplate', () => {
  it('should create an instance', () => {
    // const directive = new MaTemplate();
    // expect(directive).toBeTruthy();
  });
});
