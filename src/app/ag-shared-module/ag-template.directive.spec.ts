import { AgTemplate } from './ag-template.directive';

describe('AgTemplate', () => {
  it('should create an instance', () => {
    const directive = new AgTemplate();
    expect(directive).toBeTruthy();
  });
});
